# V25 Local Engine Boot

- Rebuilt the version wrapper.
- Uses repository-local Three.js r128 and OrbitControls r128 only.
- Removes runtime dependency on external CDN hosts.
- Embeds the local engine source into the generated inner page before startup.
- Keeps V20-V23 model, flow, hardware and smooth explosion features.
- Adds visible loading stages and 10-second fetch timeouts.
- Wrapper JavaScript passed `node --check` before upload.

Runtime on the user's mobile browser still needs confirmation after GitHub Pages deploy completes.
